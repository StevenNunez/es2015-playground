{
  "name": "es2015-playground",
  "version": "1.0.0",
  "description": "Run this to get started: ``` npm install -g babel-cli npm install ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-preset-es2015": "^6.5.0"
  }
}
